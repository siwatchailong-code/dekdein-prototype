# generated at build time by build.js — this is the Vercel
# outputDirectory; it contains the real Supabase URL + anon key in env.js
public/

# legacy path from before the build wrote into public/ — no longer produced,
# ignored in case a stray copy exists locally
env.js

# local env files
.env
.env.local

node_modules/
.DS_Store
